from django.conf.urls import url

from . import views

urlpatterns = [
    # url('libs/', views.libs, name='libs'),
    # url("queryLib/", views.query_lib, name="queryLib"),
    # url("saveLib/", views.SaveLib.as_view()),
    # url("del_lib/", views.DelLib.as_view()),
    # url("que_lib_no_ques/", views.QueLibNoQues.as_view()),
    # url("save_add_que", views.SaveAddQue.as_view()),
    # url("queryLib_ques", views.QueryLibQues.as_view()),
    # url("save_del_que", views.SaveDelQues.as_view()),
]
